import RunnerParallaxBackground from "./RunnerParallaxBackground";

const RunnerGame = () => {
  return (
    <div>
      <h1>Runner Game</h1>
      <RunnerParallaxBackground />
      <p>Return to Game Select</p>
      <p>
        This will be a separate component that will replace the caraousel when
        selected, Ive left it here so its more convenient for me to work on
      </p>
    </div>
  );
};

export default RunnerGame;
